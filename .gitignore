ansible/collections/*
!ansible/collections/.gitkeep
ansible/external-roles/*
!ansible/external-roles/.gitkeep
terraform/modules/*
!terraform/modules/.gitkeep
ansible/.ansible_vault_password
*.retry
*debug*
.terraform
.terraform.lock.hcl
.terragrunt-cache
terraform.tfstate
terraform.tfstate.backup
backend.tf
# root.hcl generate block emits this into inline-.tf unit dirs (argocd, cilium-lb);
# it embeds the live pg state conn string, so it must never be tracked.
opentofu_state_backend.tf
.envrc

# Python toolchain noise
*.pyc
__pycache__/

# Backup artifacts
*.backup.*

# OS / editor noise
.DS_Store
*.swp
